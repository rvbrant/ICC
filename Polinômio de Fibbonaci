#include <stdio.h>
#include <stdlib.h>

void Fibonacci (int n, int soma){
    if(soma > 50){
        return 0;
    }
    else{
        soma = n + soma;
        printf("%d\n", soma);
        Fibonacci(n+1, soma);
    }
}

int main(){
    Fibonacci(0, 0);
    return 0;
}
